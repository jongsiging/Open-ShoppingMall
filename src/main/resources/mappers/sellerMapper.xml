<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.seller">
<insert id="insert">
	INSERT INTO seller (seller_id, seller_pw, seller_name, seller_email, seller_phone) 
  	VALUES 
  	(#{seller_id}, #{seller_pw}, #{seller_name}, #{seller_email}, #{seller_phone})
</insert>
 
<select id="idcheck" resultType="kr.co.domain.SellerVO">
	SELECT * FROM seller WHERE seller_id = #{seller_id}
</select>
<!--
<select id="read" resultType="kr.co.domain.SellerVO">
	SELECT * FROM seller WHERE member_id = #{member_id}
</select>

<select id="updateUI" resultType="kr.co.domain.SellerVO">
		SELECT * FROM seller WHERE member_id = #{member_id}
</select>

<update id="update">
		UPDATE seller SET member_id = #{member_id}, member_email = #{member_email}, member_address = #{member_address}, 
		member_detail_address = #{member_detail_address}, member_phone_number = #{member_phone_number}, member_updatedate = sysdate 
		WHERE member_id = #{member_id} AND member_pw = #{member_pw}
</update>

<select id="login" resultType="kr.co.domain.SellerVO">
	SELECT member_id, member_name FROM seller WHERE member_id = #{member_id} AND member_pw = #{member_pw}
</select>

<select id="deleteUI" resultType="kr.co.domain.SellerVO">
		SELECT * FROM seller WHERE member_id = #{member_id}
</select>

<delete id="delete">
	DELETE FROM seller WHERE member_id = #{member_id} AND member_pw = #{member_pw}
</delete>

<select id="passChk" resultType="int">
	SELECT COUNT(*) FROM seller WHERE member_id = #{member_id} AND member_pw = #{member_pw}
</select> -->


</mapper>